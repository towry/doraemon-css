Doraemon CSS
------------

Doraemon in css. :)

### Development

You need nodejs/npm installed first, use following command to install the dependencies.

```bash
npm install
```

After installed the dependencies, run `gulp` to run the default task, then edit `less/main.less` file to make changes.

---

Copyright 2015 by Towry Wang
